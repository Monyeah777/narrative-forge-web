# NF · 死链归档（批次 1 标记版）

> 来源：《NF · 代码审查（深度）报告 v1.0》§3 清单（grep 核验 · tip `d33fb8c`）
> 批次 1（无损清理）只做**标记**，不做物理删除；物理删除放在批次 4（结构治理）。
> 标记形式：`index.html` 内 `== BATCH1-DEAD ==` 注释。

## 归档清单（现状 · 未删）

| 名称 | 位置（tip） | 状态 | 批次 1 处理 |
|---|---|---|---|
| `scheduleQuietTick` | `prototype/index.html` 主循环前的工具区 | 空 stub（仅 `clearQuietWait()`） | 保留调用点 + `== BATCH1-SHELL ==` 标记 |
| `paintVoid` | `resizeCanvas` 上方 | 空函数，仍被 `resizeCanvas` / bootstrap 调用 | 保留调用点 + `== BATCH1-SHELL ==` 标记 |
| `buildSprites / makeSprite / buildTintSprites / tintMask / sampleStops / mixRgb / gradientT` | 精灵 / 图集区 | 可达性 = 0（仅 `!pixiTrack` 分支，恒假） | 簇首标记 |
| `paintBlockDot / paintSoftDot` | 精灵区 | def-only（grep 计数 = 1） | 随簇标记 |
| `assignGlyphTint / glyphTintK` | 精灵区 | def-only | 随簇标记 |
| `closeLastPaint`（及 `lastPaintBmp`） | 精灵区 | def-only | 随簇标记 |
| `closeSprites / closeAtlas` 链 | 精灵区 | 仅留壳调用，无渲染效果 | 随簇标记 |
| worker 残：`renderWorker / workerReady / workerPending / workerHasFrame` | 状态区 / `publishRenderInfo` | 仅证据字段回显；`render-worker.js` 留仓 | 状态区标记 |
| `atlasGen` | 状态区 | 仅声明 | 随 worker 标记 |
| `raf` 变量与 `cancelAnimationFrame` 残调用 | 状态区 / 主循环 | 残留（无对应 rAF） | **已删**（T1.2，见下） |
| `WALL / WALL_K` | 常量区 | def-only（grep 计数 = 2） | 常量区标记 |
| `TINT_MS / TINT_STEPS / STOP_N / STOP_F / GLYPH_*` | 常量区 | 半死（仅死链引用） | 常量区标记 |
| `?block / ?pixel` 开关 | manifest 查询段 | 半失效（见 F-06 / 卷面 O2） | 不动，等裁决 |

## 批次 4 · 物理删除执行记录（2026-09-13）

按上表逐项执行；每项删除前先全仓 grep 核验引用数，删后 `node --check` + 浏览器冒烟，批次末跑金测 / 全闸 / 7 步。证据 `painting/codex-batch4.json`。

| 已删 | 处置 |
|---|---|
| `scheduleQuietTick` 及其调用点、`quietWait`、`clearQuietWait` | 全链删除（RENDERER-SWAP 后无路径会把 `quietWait` 置非 0，`!quietWait` 恒真） |
| `paintVoid` 及 `resizeCanvas` / bootstrap 两处调用 | 删除 |
| `makeSprite / paintBlockDot / paintSoftDot / closeLastPaint / closeSprites / closeAtlas / mixRgb / sampleStops / gradientT / tintMask / buildTintSprites / assignGlyphTint / glyphTintK / buildSprites` | 整簇删除（可达性 0：只在 `!pixiTrack` 分支被调） |
| `lastPaintBmp / sprites / tintSprites / spriteBmp / atlasGen / gid / tid` | 随簇删除 |
| `renderWorker / workerReady / workerPending / workerHasFrame` | 删除；`__nfRender.workerBlit` / `workerHasFrame` 改为固定 `false`（原本恒为 false） |
| `snapAllToTargets` | 删除（批次 3 后无调用方）；`handoverSnapCalls` 保留为证据字段 |
| `WALL / WALL_K` | 删除（grep 计数 = 2，无消费方） |
| `TINT_MS / TINT_STEPS / STOP_N / STOP_F / GLYPH_N / GLYPH_F` | 删除；`GLYPH_SPLIT` 因仍被字形分类使用，改为原式内联保留 |
| 5 处不可达 `try/catch`（`usesPointsBin` / `usesRadial` / `usesAssemblyMorph` / `loadTargetRecord` / `dpr+fx`） | 删除（`NF_QUERY.get` 不会抛） |
| `lutFilSlice`（physics）/ `lastPalette` / `lastPointsId`（renderer） | 跨文件残留一并删除 |

**保留项（有意不删）**：`buildPaletteAtlas` / `assignPaletteSizes` / `rebuildBuckets` 及其桶数组、`atlasBmp` / `atlasCell` / `atlasPalN`（仍被 `resizeCanvas`、目标装载、`publishRenderInfo` 使用）；`GLYPH_SPLIT`；`prototype/render-worker.js` 文件（按 HANDOFF 留仓）。

**规模**：`prototype/index.html` 4606 → 4376 行（本批 −230）；对 tip 累计 +207/−324（净 −117）。

## 批次 1 已落地的等价改动

- **T1.1 查询闸缓存（F-03）**：`prototype/index.html` 新增 `NF_QUERY`（一次解析 + 惰性缓存），替换 `dpr` / `fx` / manifest 段 / `usesPointsBin` / `usesRadial` / `usesAssemblyMorph` / `nf-points` 共 7 处 `new URLSearchParams(...)`。`.get(k)` 语义与旧 `catch` 分支保持等价。
- **T1.2 循环入口合并（F-08）**：`wakeLoop` 改为 `startLoop` 的薄包装（两个入口名都保留）；删除 `raf` 残留变量与 3 处 `cancelAnimationFrame` 残调用。
- **T1.3 空壳处理**：`paintVoid` / `scheduleQuietTick` 保留调用点并加 `== BATCH1-SHELL ==` 注释。

## 批次 4 待办（不在本批）

1. 物理删除上表全部条目（先 grep 全仓 + 金测）。
2. `render-worker.js` 与 `prototype/render-worker.js` 的去留随 worker 残链一起裁决。
3. 删除后必须复跑：C2 golden / NFPT 头 / S3 dual / vendor sha + 7 步 + 全闸快照。

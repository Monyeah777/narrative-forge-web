# 径向重映射 · S4 渲染

手册：`docs/NF_径向重映射_总结版最终方案_v2.0.md`  
模块：`prototype/pixi-physics/nf-radial-render.js`  
脚本：`scripts/radial-s4-render.mjs`  
证据：`painting/radial-s4.json`  
检索：`docs/research/径向重映射-备忘.md`（S4 段）

本闸 = §5 菜单 **A→D 每步 bench**。**没有**挂现网 ticker。**没有**写现网 bin。**没有**改 `nf-spring.js` / `applySpring` / C2 golden。**没有**替换 `renderer.pixi.js`。采样 ②/③/⑤ 继续休眠。菜单 **E 未启用**。

## 检索（过闸后才写代码）

§7#5 五搜。引用 MDN `putImageData`、OffscreenCanvas、Firefox 1081887、web.dev OffscreenCanvas、WHATWG canvas。详见备忘。

脏矩形坐标相对 **ImageData 源**（MDN）。Firefox 1081887：脏参历史上仍可能整幅预乘——B 同时记 `upload_px` 与 `full_px`，不把脏参当免费。

## 像素块（继承，未改规格）

| 项 | 值 |
|---|---|
| 形状 | 哑光实心方块 |
| 尺寸 | **2** 设备像素 |
| 对齐 | `Math.round` 后以块中心贴整数格 |
| 平滑 | 关（软件帧缓冲，无插值） |
| 发光 | 禁（不透明 `A=255`） |
| 墨 | 隔离单色 `#C9CFD8`（现网 256 色盘未动） |
| 底 | `#050505` |

幅面复用 S3 Dallas 场：1200×1540，`c*=(0.5,0.5)`，`R95=0.599042`。点集复读锁定的 S2b Dallas u16（n=50601）。

## 菜单

| 步 | 开 | 做什么 |
|---|---|---|
| none | — | 每帧清全幅 + 画全部 50k |
| A | 中心烘焙 | `r<0.55`（`A(r)=0`）写入 `base`；DWELL 只擦/画运动点 |
| A+B | + 脏矩形 | 擦+画块的并集 bbox；`upload_px = dirtyW·dirtyH` |
| A+B+C | + 噪声 LUT | blit 走 `world.renderXY`（64×64@30Hz + 双线性）；关则逐点 Simplex |
| A+B+C+D | + 边缘分时 | `r≥0.85` 按 index 奇偶隔帧更新，未到帧的点保持上次画位 |
| E | 申议 | 降 count / WebGL。本闸 **不启用**。穷尽 A–D 仍超才记账，等拍板 |

Node 22 无 canvas：`Uint32Array` 软件帧缓冲模拟 ImageData（复用一块，打包写）。确定性双跑哈希的是像素，不是毫秒。

## 预算

- 手册 §5：DWELL **脚本增量 ≤ +1.0ms**（相对 B4.1 dwell p95 0.20ms）
- 修订包停留：脚本 ≤ **2ms** / 帧，≤30Hz
- 本云 VM 是软件帧缓冲，**不是**作者机标尺（与 S0 相同）。超支只申议，不静默砍 N、不改现网 100k

本云 VM 软件帧缓冲（不是作者机）。Dallas 50k 锁定点：烘焙 23565 / 运动 20175 / 边缘 6861。

| 步 | script p95 | blit p95 | upload_px | 相对 none |
|---|---:|---:|---:|---|
| none | 8.97ms | 8.46ms | 1 848 000 全幅 | — |
| A | 8.72ms | 7.61ms | 全幅（B 关） | blit 约 −0.8ms |
| A+B | 9.16ms | 8.04ms | **仍全幅** | 并集 bbox 铺满画；脏参无减 |
| A+B+C | 6.35ms | 5.17ms | 全幅 | LUT 是本闸最大一刀 |
| A+B+C+D | **6.48ms** | 5.36ms | 全幅 | 边缘隔帧，held=3322；D 增量在噪声里 |

B 的单框脏矩形盖不住「铺满幅面的 2.7 万个 2×2 块」。`stamp_px`（擦+画块面积、含重叠）= **216 288**，但 `putImageData` 只收一个矩形。这不是实现错，也不因此私自改成多框/分块——那不在 A–D 里。

相对 B4.1 dwell p95 0.20ms：A–D 脚本增量 **+6.28ms**，过 +1.0ms，也过修订包 2ms。**申议 E，未启用。** 未砍 50k，未改现网 Pixi。数字以 `painting/radial-s4.json` 为准。

双跑哈希过：`a80180be…e08ae`（A+B+C+D）/ `d038e6ed…8c14`（none）。

## §8

| # | 本闸 |
|---|---|
| 7 | 指标未进预算。协议守：A→D 分步 + 证据申议 E，未静默超支、未砍 N |
| 8 | **过** `a80180be…e08ae` |
| 1–2 | S2c 锁定；不重采样 |
| 3–6 | S3 锁定；不改 `k(r)` / `c*` |
| 9 | 留给 S5；现网 ticker 未挂 |

## 现网未动

Dallas `6406d701…ea82` / Scotland `bbeab899…cf5f` / Met `3144fdb9…9a45` / C2 `dc1f1609…320a`。`SPRING_SHARP = 0.055`。Pixi `ParticleContainer` 仍是现网默认。

## 本闸未做

S5 总验收、15s 视频、挂现网 ticker、写现网 50k、改现网 0.055、启用 E、把现网改回 Canvas2D。

# 径向重映射 · S2c 质检

手册：`docs/NF_径向重映射_总结版最终方案_v2.0.md`  
脚本：`scripts/radial-s2c-qc.py`  
证据：`painting/radial-s2c.json`  
对象：锁定的 S2b 隔离 u16（**再读文件，不重采样**）

本闸 = NN / 密度比 / 可选频谱。**没有**开 ②/⑤。**没有**写现网 bin。弹簧 / 调色板未动。§8 #3–#7 / #9 是 S3–S5。

## 做法

1. 读 `painting/radial-s2b-*-50k.u16`，sha 必须等于 S2b 记录。
2. 用 S0 同一场复测分带密度与 NN（像素距离）。
3. 可选频谱：256² 直方图 → FFT。全图会看见 `W(r)` 的低频，不当缺陷。蓝噪声看 **r∈[0.3,0.6) 内的填满方窗**（避免圆环窗假峰）。
4. 目检：分带总图 + 中心/边缘放大。

## §8 复测

| 样本 | n | 中心/边缘 | #1 ≥2.0× | NN p99/median | #2 ≤1.8 | 16px 空格 | u16=S2b |
|---|---:|---:|---|---:|---|---:|---|
| Dallas | 50601 | 2.4241 | **是** | 1.457 | **是** | 0 | 是 |
| Scotland | 50441 | 2.6127 | **是** | 1.550 | **是** | 0 | 是 |
| Met | 50471 | 2.6351 | **是** | 1.553 | **是** | 0 | 是 |

#8 双跑哈希：本闸复读 S2b 已锁定文件，sha 一致。NN 与 S2b 差 0.001 量级，来自 u16 往返。

## 可选 #10 频谱（目检为主）

| 窗 | 观察 |
|---|---|
| 全图 FFT | 中心亮斑 = `W(r)` 径向场。`low_over_mid` 很大，**预期如此**。 |
| 中带方窗 FFT | 中心暗、无点阵亮点。轴向/旁瓣 Dallas **0.87** / Scotland 0.73 / Met 0.74（<3，无栅格峰）。 |
| 中心放大 | 间距匀，无空圆。 |
| 边缘放大 | 更疏，仍无空圆；右侧空是 R95 场边界。 |

径向平均曲线是高频抬升，不是教科书单环。档① 足够过 #1/#2；不因此升级。

## 升级

**不触发。** #1/#2 仍过。② / ③ / ⑤ 继续休眠。

## 现网未动

Dallas `6406d701…ea82` / Scotland `bbeab899…cf5f` / Met `3144fdb9…9a45`。

## 本闸未做

S3 物理、S4 渲染、S5 总验收、写现网 bin、改弹簧。

# 径向重映射 · S5 总验收

手册：`docs/NF_径向重映射_总结版最终方案_v2.0.md`  
脚本：`scripts/radial-s5-accept.mjs`  
证据：`painting/radial-s5.json`  
检索：`docs/research/径向重映射-备忘.md`（S5 段）

本闸 = §8 全表 + 15s 视频 + 放大对比 + 帧时曲线 + 回退清单 + 偏差对照。**复读** S2c / S3 / S4 锁定证据，不重采样，不改 `k(r)` / `c*`，不替换现网 Pixi，不写现网 bin，**不启用 E**。

## 检索（过闸后才写代码）

§7#7：Ulichney 径向谱、Schlömer & Deussen 2011、Lagae & Dutré。方法与 S2c 相同：NN + 分带密度 + 中带方窗 FFT。详见备忘。

## §8 全表

数字以 `painting/radial-s5.json` 为准。

| # | 指标 | 来源 | 本闸 |
|---|---|---|---|
| 1 | 中心/边缘 ≥2.0× | S2c 复读 | **过** 2.42 / 2.61 / 2.64 |
| 2 | NN p99 ≤1.8×median | S2c 复读 | **过** |
| 3 | 飘逸 edge≥3×center | S3 | **过** 中心 0 / 边缘 std 1.12 |
| 4 | 单帧 p99 ≤0.5px | S3 | **过** 0.037px |
| 5 | 复位 中心≤1s / 边缘≤1.5s | S3 + 本闸全点 +24px | **过** 0.42s / 1.00s |
| 6 | 60/120 时差 ≤10% | S3 | **过** 0% |
| 7 | B4.1 + §5 | S4 | **预算未进**（+6.28ms）；协议守；E 申议未启用 |
| 8 | 双跑哈希 | S2c/S3/S4 + 本闸相位序 | **过** `1ca653c9…747e` |
| 9 | 驱离/重组/状态机 | 本闸隔离相位 | **过** INTRO/HANDOVER Δ=0；DWELL 边缘 mean 2.55；REPEL 比 0.50；唤醒；复位 |
| 10 | 频谱（可选） | S2c 中带方窗 | **过** 无栅格峰；不升级 |

#9 **不是**现网 `applySpring` 驱离。隔离模块没有 100px 平方反比。本闸验的是手册 §4 状态门控 + 休眠唤醒 + 复位。现网状态机未挂本模块。

## 交付物

- 15s 录像：`painting/radial-s5-dwell-15s.mp4`（左中心 2× | 右边缘 2×，960×512；0–2s INTRO / 2–4s HANDOVER / 4–12s DWELL / 12–15s REPEL）。右半 1s 与 8s 像素哈希不同（最大通道差 220）。全幅 3× 会把 ≤5px 飘逸吃掉，所以录像走裁切放大，不是改物理。
- 放大：`radial-s5-full.png`、`radial-s5-zoom-center.png`、`radial-s5-zoom-edge.png`
- 帧时：`radial-s5-frametime.svg`（隔离 drain+blit，本云 VM，非作者机）
- 回退 / 偏差：见 JSON `rollback` / `deviation`

## 现网未动

Dallas `6406d701…ea82` / Scotland `bbeab899…cf5f` / Met `3144fdb9…9a45` / C2 `dc1f1609…320a`。`SPRING_SHARP = 0.055`。Pixi 仍是默认。

## 手册闸之后

S0–S5 已齐。未再听到点名，不得：写现网 50k、启用 E、把隔离模块挂进 `index.html`。只说「继续」也不够改现网点数量。
